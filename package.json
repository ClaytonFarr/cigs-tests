{
  "name": "cigs-workflow-tests",
  "scripts": {
    "generate": "tsc && node dist/generate.js",
    "schema": "tsc && node dist/schema.js",
    "classify": "tsc && node dist/classify.js",
    "handler": "tsc && node dist/handler.js",
    "uses": "tsc && node dist/uses.js"
  },
  "devDependencies": {
    "@types/node": "^14.14.31",
    "typescript": "^4.2.3"
  },
  "dependencies": {
    "cigs": "^0.1.4"
  }
}
